{
  "name": "webapp",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "dev": "meteor run --settings development.settings.json"
  },
  "dependencies": {
    "@babel/runtime": "^7.3.4",
    "meteor-node-stubs": "^0.4.1"
  }
}
