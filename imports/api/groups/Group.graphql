type Group {
  id: ID
  title: String
  participants: [Participant]
}

input GroupInput {
  name: String!
}

extend type Query {
  group(id: ID!): Group
}

extend type Mutation {
  createGroup(group: GroupInput!): Group
}
